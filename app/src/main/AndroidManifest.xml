<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myphotoapp">


    <!--위험 권한 / 저장-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />    <!--위험 권한 / 저장-->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />          <!--위험 권한 / 전화-->
    <uses-permission android:name="android.permission.CALL_PHONE" />                <!--위험 권한 / 전화-->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".mainApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailActivity"/>
        <activity android:name=".DogView.DogAddActivity"/>
        <activity android:name=".Fragment.Chatroom"/>

    </application>




</manifest>